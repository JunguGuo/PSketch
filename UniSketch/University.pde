class University {
  float lat,lng;
  int rank;
  String name;
  
  //boolean over = false;
  
  // Create  the Bubble
  University(float lat_, float lng_, int rank_, String s) {
    lat = lat_;
    lng = lng_;
    rank = rank_;
    name = s;
  }
  

  
  // Display the Bubble
  void display() {
    //stroke(0);
    //strokeWeight(2);
    //noFill();
    //ellipse(x,y,diameter,diameter);
    //if (over) {
    //  fill(0);
    //  textAlign(CENTER);
    //  text(name,x,y+diameter/2+20);
    //}
  }
}
